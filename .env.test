# API Configuration
API_URL=http://localhost:5055

# Supabase Configuration - You need to set these with your actual Supabase credentials
# Option 1: Supabase API endpoint (cloud service)
# SUPABASE_URL="https://your-project.supabase.co"
# SUPABASE_ANON_KEY="your-anon-key"

# Option 2: PostgreSQL connection string (local or self-hosted)
SUPABASE_URL=postgresql://postgres:postgres@localhost:5432/open_notebook
SUPABASE_ANON_KEY=anon_key

# For now, we'll use a local PostgreSQL connection since we don't have Supabase running
# The application should handle this gracefully with error messages